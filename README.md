# IESPFLIX (backend) - scaffold

Projeto inicial scaffoldado com entidades JPA, repositórios, validação custom e migration Flyway.

Run:

```bash
mvn spring-boot:run
```

H2 console can be enabled if needed in `application.yml`.
